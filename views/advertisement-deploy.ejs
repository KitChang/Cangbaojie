      <div class="container content">
        <div class="row">
            <div class="panel panel-dashboard full-width">
                <ol class="breadcrumb panel-heading panel-heading-cbj">
                  <li><a href="/advertisement">广告</a></li>
                  <li class="active">广告发送</li>
                </ol>
                <div class="panel-body">
                    <button onClick="javascript: window.location.href='/advertisement/<%=result.id%>/device'" class="btn btn-primary">设备发送</button>
                    <button onClick="javascript: window.history.back();" class="btn btn-default">取消</button>
                </div>
                <form name="form" method="post">
                <table class="table table-striped" id="table">
                    <thead>
                    <tr><th>已发送的设备</th><th>省</th><th>城市</th><th>地区</th><th>街道</th><th>位置</th><th>位置类</th></tr>
                    </thead>
                    <tbody>
                <%
                var row;
                if(devices!=null)
                while (devices.length) {
                  row = devices.pop();
                %>
                    <tr>
                    <td><%=row.identifier%></td>
                    <td><%=row.state%></td>
                    <td><%=row.city%></td>
                    <td><%=row.region%></td>
                    <td><%=row.street%></td>
                    <td><%=row.location%></td>
                    <td><%=row.locationType%></td>
                    </tr>
                <%
                }
                %>
                    </tbody>
                </table>
                </form>
            </div>
        </div>
      </div>
    <script type="text/javascript" src="/js/dependencies/jquery.min.js"></script>
    <script type="text/javascript" src="/bootstrap/js/bootstrap.min.js"></script>
    <script type="text/javascript" src="//cdn.datatables.net/1.10.10/js/jquery.dataTables.min.js"></script>
    <script>
    $("#table").DataTable({"language": {
            "url": "//cdn.datatables.net/plug-ins/1.10.10/i18n/Chinese.json"
        }});
    </script>